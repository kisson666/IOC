在网上看了几天的ioc原理之后，按着自己的理解和网上的例子，写了一个ioc容器，但是实现的功能只是spring的一小部分，仅仅能给
bean注入属性而已。其实原理很简单，就是利用反射。


 用dom4j这个工具包可以解析xml文件，然后解析出里面的bean id class property value 等等一系列的结点或者属性，就能得到需要的class名字，和需要注入的值了
 利用反射，利用得到的class的名字加载对应的类，然后根据得到的需要注入的属性的名字，拼接加上set，利用反射的method类 找到对应的set方法 调用menthod.invoke
方法执行set方法 执行完所有对应的set方法之后就把这个实例存入map中，对应的id就是xml文件中<bean id=?>中的?


xml文件有多个bean的话就利用循环就可以了



其实没有之前想象得那么难 只是原来自己把解析xml文件这个步骤想成了需要自己写
网上百度以后 发现有dom4j这种工具包 用起来真是太爽了

但是还有一个问题，就是我还不知道怎么获取相应set方法的参数，就是javabean需要注入的属性的类型，用反射的getMethod(1,2)第一个参数是方法的名字，可以用set拼接xml文件中
属性的名字，第二个就是set方法的参数，我不知道怎么获取，只能把它定义成string 写死，这显然是不行的，如果属性是int类型或者其他的 该怎么办？
